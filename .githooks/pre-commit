#!/bin/sh
email="$(git config --local user.email)"
if [ "$email" != "jonykoren@gmail.com" ]; then
  echo "This repo requires git user.email=jonykoren@gmail.com (current: ${email:-unset})."
  echo "Run: git config --local user.email jonykoren@gmail.com"
  exit 1
fi
